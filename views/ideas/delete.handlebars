<div class="container bg-gray-300 shadow w-1/2 mx-auto my-2 p-3">
    <h1 class="text-center text-red-500">
        Are you sure you want to delete idea "{{idea.title}}" ?
    </h1>
    <form class="mt-2 flex justify-center" action="/ideas/delete/{{idea._id}}" method="post">
        <button type="submit" class="inline-block m-1 px-6 py-2 text-xs font-medium leading-6 text-center text-white uppercase transition bg-pink-500 rounded shadow ripple hover:shadow-lg hover:bg-pink-600 focus:outline-none">Delete</button>
    </form>
</div>